def fibonacci_generator(n=5):
    """Generate first n Fibonacci numbers."""
    a, b = 0, 1
    count = 0
    while count < n:
        yield a
        a, b = b, a + b
        count += 1

# Print first 5 Fibonacci numbers
for num in fibonacci_generator(5):
print(num, end=" ")
